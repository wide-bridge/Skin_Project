# Next Actions

1. `05` 피부질환 RAG corpus를 `label_data` 기준 실제 데이터로 확장
2. `05`와 연결될 `rag_corpus_derma.jsonl` 정제 규칙 확정
3. `06` 챗봇 전체 구현이 아니라 진단 결과 연계 응답 계약만 정리
4. `/diagnosis/infer` 업로드 파일 계약과 얼굴 사전검사 훅 설계
5. 이후 Qwen 계열 VLM/LoRA adapter 실제 연결 준비
